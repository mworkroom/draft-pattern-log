# 2026-09-23 SOP Score Tracker Development Log

## Work Summary

첨부된 SOP Score Tracker 계획서와 시안을 기반으로, 감수 직후 빠르게 기록하고 언어별 구조·수정 패턴을 비교할 수 있는 데스크톱 웹앱을 구현했다. J님이 정정한 Time Spent 기본값은 60m로 반영했다.

## Work Completed

- React·Vite·TypeScript·Tailwind 기반의 단일 화면을 만들고 입력 폼, Dashboard, 분석 탭, 최근 기록 테이블을 구현했다.
- Draft Language는 English, Level은 Master's, School Tier는 Mid, AI Usage는 Yes, Time Spent는 60m로 시작하고 저장 후 새 폼에도 같은 기본값을 적용했다.
- Structure 8개 점수, Major Rework 7개, Problem Type과 Unclassified 메모 검증을 구현했다. Type 1과 Type 2는 동시에 선택되지 않도록 했다.
- 감수일과 생성·수정 시각을 분리하고, 중앙값·항목별 평균·수정 비율·복합 필터를 구현했다.
- localStorage 저장, 수정·삭제, JSON 백업·복원, CSV 내보내기와 백업 필요 표시를 구현했다. 손상된 데이터는 자동으로 덮어쓰지 않도록 했다.
- GitHub Pages 배포 워크플로와 사용 안내를 추가했다.
- 단위 테스트 5개와 Chrome 브라우저 회귀 테스트 2개를 추가했다. TypeScript·프로덕션 빌드, Chrome 1674×941 및 1280×900 브라우저 동작을 확인했다.

## Decisions

- 개인용 한 PC 앱이라 서버 없이 브라우저 저장을 사용했다. 나중에 저장 방식이 바뀌어도 기록 구조를 유지할 수 있도록 JSON 백업에 스키마 버전을 넣었다.
- 영어·한국어 비교에서 총점과 재작업 건수는 중앙값, Structure 개별 항목은 소수점 첫째 자리 평균으로 표시했다. 시간 구간은 실제 분의 평균으로 환산하지 않고 순서형 중앙 구간으로 표시했다.
- 시안에 없는 Problem Type·백업 기능은 입력 속도와 시안의 밀도를 유지하도록 기존 카드·버튼 스타일로 확장했다. 작은 화면에서는 저장 바를 두 줄로 배치했다.
- 초기 시각 검증에서 저장 바가 화면 아래로 밀리는 문제를 발견해 앱 높이와 독립 스크롤을 수정했다. 브라우저의 favicon 404 오류도 데이터 URL 아이콘으로 해결했다.
- npm 감사에서 확인된 개발용 Vitest 취약 버전을 수정 버전으로 올렸고 재검사에서 취약점 0건을 확인했다.
- Playwright 테스트 파일 추가 후 Vitest가 이를 단위 테스트로도 수집해 실패했다. Vitest의 수집 범위를 src의 단위 테스트로 제한하고 두 테스트 명령을 다시 통과시켰다.

## Current Status

로컬 Chrome에서 기본값, 필수값 검증, 저장·필터·수정·삭제·JSON 백업·복원·새로고침 후 보존을 확인했다. 1280×900에서 저장 버튼이 보이고 페이지 전체 가로 넘침이 없다. GitHub Pages Actions 배포가 성공했고 공개 주소를 Chrome에서 열어 화면이 정상 렌더링되는 것을 확인했다. J님의 실제 감수 입력 시간은 아직 확인 전이다. Actions는 기존 버전 액션의 Node 20 지원 종료 경고를 표시했지만 이번 빌드와 배포에는 영향을 주지 않았다.

## Next Steps

- J님이 실제 감수 사례로 한 건을 입력해 30초 목표와 항목 설명의 적합성을 확인한다.
- GitHub Actions의 Node 20 경고는 해당 액션의 새 버전이 제공될 때 갱신한다.

## Work Summary - Chrome 100% 가독성

Windows 표시 배율 125%를 유지하면서 Chrome 확대 100%에서 앱을 읽기 쉽도록 작은 UI 글씨를 조정했다.

## Work Completed - Chrome 100% 가독성

- 입력·버튼·점수·체크 항목과 대시보드 주요 레이블을 14~16px 중심으로 키웠고, 보조 설명도 대체로 12~13px로 올렸다. 입력칸과 선택 버튼의 높이도 함께 늘렸다.
- 1600px 이하에서는 점수/재작업 영역을 세로로, KPI는 3열·차트는 2열로 재배치해 확대된 글씨가 좁은 칸에 눌리지 않게 했다.
- 1280px·2048px Chrome 뷰포트에서 주요 글씨 크기, 저장 버튼 노출, 페이지 가로 넘침을 확인하는 브라우저 회귀 테스트 2개를 추가했다.
- 프로덕션 빌드, 단위 테스트 5개, Chrome 브라우저 테스트 4개가 통과했고 두 뷰포트의 스크린샷을 직접 확인했다.

## Decisions - Chrome 100% 가독성

- 브라우저 확대를 강제하지 않고 앱 자체 글씨·터치 영역을 키웠다. 좁은 화면에서는 이전의 9px 축소 규칙을 후행 가독성 규칙으로 덮고 카드 열 수를 줄여 읽기 크기를 유지했다.

## Current Status - Chrome 100% 가독성

로컬 변경과 자동·시각 검증은 완료했다. 실제 J님 모니터에서의 체감 가독성은 아직 확인 전이다.

## Next Steps - Chrome 100% 가독성

- 배포 후 J님 환경의 Chrome 100%에서 글씨와 스크롤 양이 적절한지 확인한다.

## Work Summary - 가독성 프로젝트 지침

작은 글씨 때문에 같은 UI 작업을 반복하지 않도록, 폰트와 레이아웃 검증 기준을 프로젝트의 `AGENTS.md`에 명시했다.

## Work Completed - 가독성 프로젝트 지침

- 프로젝트 루트에 `AGENTS.md`를 추가하고 J님의 Windows 125%·Chrome 100% 사용 환경, 권장 글씨 크기, 2048 px·1280 px 화면 검증 기준을 기록했다.

## Decisions - 가독성 프로젝트 지침

- 개인적인 연령·건강 상태가 아니라 실제 사용 조건과 읽기 편한 UI 기준을 적었다. 좁은 화면에서도 글씨를 줄이기보다 배치를 바꾸도록 했다.

## Current Status - 가독성 프로젝트 지침

지침 파일은 작성했으며, 실행 코드나 기존 UI 동작은 변경하지 않았다.

## Next Steps - 가독성 프로젝트 지침

- 이후 UI 변경 때 이 기준으로 Chrome 100% 화면을 확인한다.

## Work Summary - 가독성 지침 위치 정정

가독성 기준이 모든 앱에 적용되는 공통 선호사항임을 확인하고, 프로젝트별 파일 대신 전역 지침으로 옮겼다.

## Work Completed - 가독성 지침 위치 정정

- `C:\Users\Marion\.codex\AGENTS.md`에 Chrome 100% 가독성, 권장 글씨 크기, 화면 검증 기준을 추가했다.
- 중복 관리와 기준 불일치를 막기 위해 앞서 추가한 프로젝트 루트 `AGENTS.md`를 제거했다. 실행 코드와 UI는 변경하지 않았다.

## Decisions - 가독성 지침 위치 정정

- 사용 환경과 읽기 편한 기본 크기는 SOP Score Tracker만의 요구가 아니므로 전역 지침을 단일 기준으로 삼는다.

## Current Status - 가독성 지침 위치 정정

전역 파일에서 새 지침을 확인했다. 프로젝트 저장소에는 중복 지침이 남지 않는다.

## Next Steps - 가독성 지침 위치 정정

- 이후 앱 UI 작업에서 전역 가독성 기준을 기본값으로 적용한다.

## Work Summary - 폴더 자동 JSON 백업

브라우저 저장공간 삭제로 기록을 잃을 수 있다는 문제를 해결하기 위해, 사용자가 선택한 폴더에 기록 JSON을 자동 보관하고 파일에서 복구하는 흐름을 추가했다.

## Work Completed - 폴더 자동 JSON 백업

- 처음 한 번 Documents의 EDM 폴더를 선택하도록 안내하고, 폴더 핸들을 IndexedDB에 기억한다. 재방문 시 권한을 확인하고 필요하면 다시 허용받는다.
- 기록 저장·수정·삭제·JSON 복원 후 최신 JSON을 자동 갱신한다. 그날 첫 덮어쓰기 전 파일과 충돌 해결 전 파일은 별도 복구본으로 보존한다.
- 브라우저 기록과 폴더 파일이 다르면 자동 덮어쓰기를 중단하고, 파일에서 복구하거나 브라우저 기록을 파일로 저장하도록 선택하게 했다. 자동 백업 성공·실패 상태와 수동 백업 대안을 화면에 표시했다.
- README 사용·복구 안내를 갱신했다. 프로덕션 빌드, 단위 테스트 5개, Chrome 브라우저 테스트 5개가 통과했고 2048×1000·1280×900 화면을 확인했다.

## Decisions - 폴더 자동 JSON 백업

- 브라우저 보안상 절대 경로를 코드로 직접 지정할 수 없으므로, 폴더는 사용자가 선택하고 앱은 그 권한을 재사용한다. 공개 UI·코드에는 Windows 사용자명 대신 Documents/EDM만 안내한다.
- EDM이 아닌 이름의 폴더를 고르면 한 번 더 확인하되, 경로 이동 등 예외 상황을 위해 사용자가 의도적으로 다른 폴더를 선택할 수 있게 했다.
- 시작 시 파일이 현재 기록과 다르면 빈 브라우저 데이터로 기존 JSON을 덮어쓰지 않는다. 실제 파일 쓰기가 성공한 뒤에만 자동 백업 완료로 표시한다.
- 테스트의 가상 OPFS 폴더 핸들을 IndexedDB에 보관한 채 새로고침하면 테스트 Chrome 세션이 닫혀, 해당 테스트는 핸들 소실 후 폴더 재선택·복구를 검증하도록 조정했다. 실제 OS 폴더 권한 지속성은 별도 확인이 필요하다.

## Current Status - 폴더 자동 JSON 백업

로컬 자동·시각 검증은 완료했다. J님의 실제 EDM 폴더에 대한 최초 선택, Chrome 권한 재사용, 디스크 파일 확인은 아직 진행하지 않았다.

## Next Steps - 폴더 자동 JSON 백업

- 배포 후 J님이 앱에서 EDM을 한 번 선택하고, 테스트 기록 저장 후 `sop-score-tracker-current.json` 생성 및 재실행 시 연결 상태를 확인한다.

## Work Summary - Field 선택형 분류

반복 입력을 줄이고 지원 과정별 SOP 문제를 비교하기 쉽도록 Field 자유 입력을 드롭다운으로 바꿨다.

## Work Completed - Field 선택형 분류

- Business / Management, Education, 공대·이공계, Social Sciences, Sport, Development Studies, International Relations, Other의 8개 선택지를 `src/model.ts`의 `FIELD_OPTIONS` 한곳에 정의했다. 뒤의 두 분야는 Social Sciences와 별도 선택지다.
- 새 기록에서는 빈 선택 상태로 시작하고, 기존 자유 입력 기록을 수정할 때는 이전 값을 `Existing: ...` 선택지로 보여 준다. 새 분류를 고르기 전까지는 원래 값을 유지한다.
- 드롭다운에도 기존 입력칸과 같은 15px 글씨·40px 높이를 적용했다. 단위 테스트 7개, Chrome 브라우저 테스트 6개, 프로덕션 빌드가 통과했다.
- Chrome 100%의 2048×1000·1280×900 화면에서 Field 선택·콘솔 오류·가로 넘침·스크린샷을 확인했다. 두 화면 모두 Field 글씨가 15px이고 페이지 가로 넘침과 관련 오류가 없었다.

## Decisions - Field 선택형 분류

- 분류 기준은 과거 직업이 아닌 현재 지원 과정이다. 정해진 8개 값은 한 상수에 두어 이후 항목 추가·순서 변경이 쉽도록 했다.
- 기존 `field` 문자열과 JSON 백업 스키마를 유지했다. 예전 학과명을 임의로 새 그룹에 자동 매핑하면 잘못 분류할 수 있으므로 기존 기록은 편집할 때 직접 재분류하도록 했다.
- Field의 기존 선택 사항 성격은 유지했다. Dashboard Field 필터는 저장된 값을 기준으로 동작해 새 선택지와 예전 입력값을 모두 찾을 수 있다.

## Current Status - Field 선택형 분류

코드 변경과 로컬 자동·시각 검증은 완료했다. 실제 EDM 폴더 및 배포 앱의 동작은 이번 작업에서 다시 검증하지 않았다. Browser 플러그인이 없어 설치된 Chrome을 Playwright로 검증했으며, 처음 테스트 시도에서는 샌드박스의 Vite/Vitest 설정 읽기 거부가 발생해 권한을 높여 다시 실행했다.

## Next Steps - Field 선택형 분류

- 기존 3건의 Field가 자유 입력값이라면 각 기록을 열어 해당 그룹으로 선택한다. 이후 새 분류가 필요하면 `FIELD_OPTIONS`에 항목 한 줄을 추가한다.

## Work Summary - Revision Focus

문서 전체의 구조 문제 유형과 실제 감수에서 섹션별로 수행한 수정 정도를 구분하기 위해 Revision Focus를 추가했다.

## Work Completed - Revision Focus

- Problem Types 아래에 Academic Plan, Conclusion, Experience Closing의 None·Refine·Rebuild 라디오 표를 추가했다. 세 항목은 Problem Types 및 Major Rework와 독립적으로 저장된다.
- 각 기록에 `revision_academic_plan`, `revision_conclusion`, `revision_experience_closing`을 저장하고, localStorage·수동 및 폴더 JSON 백업·CSV 내보내기에 포함했다.
- 과거 기록이나 백업에 필드가 빠졌을 때만 `none`으로 보정한다. 잘못된 명시적 값은 거부한다. Dashboard의 Problem patterns 탭에 현재 필터 기준 건수·비율 통계를 추가했다.
- 이전 Field 선택지를 기대하던 테스트를 현재 목록에 맞췄다. 단위 테스트 10개, Chrome 브라우저 테스트 8개, 프로덕션 빌드가 통과했다. 1280px·2048px에서 새 표의 글씨 크기, 가로 넘침, 화면 배치와 콘솔 상태를 확인했다.

## Decisions - Revision Focus

- 첨부 이미지처럼 세 영역을 행으로 배치하고 한 행에서 하나만 선택되는 기본 라디오 버튼을 사용했다. 설명은 각 선택지의 제목에 담고 본문 표 글씨는 14px로 유지했다.
- 기존 JSON 스키마 버전과 저장소 키를 유지하고 읽을 때 빠진 값만 보정해, 과거 데이터를 임의로 덮어쓰지 않으면서 이전 백업을 열 수 있게 했다.
- 통계는 Problem Type이나 Major Rework에서 추론하지 않고 각 기록의 Revision Focus 값만 집계한다. 필터 결과가 0건이면 비율 대신 데이터 없음으로 표시한다.

## Current Status - Revision Focus

기능과 로컬 자동·시각 검증을 완료했다. J님의 실제 저장 기록과 EDM 폴더 파일, 배포된 앱에서는 아직 확인하지 않았다.

## Next Steps - Revision Focus

- 배포 후 기존 기록이 None으로 열리는지, 실제 기록을 수정했을 때 연결된 EDM 폴더 백업에 새 필드가 기록되는지 확인한다.

## Work Summary - Major Rework 중복 제거와 Revision Focus 순서

Revision Focus와 같은 의미로 입력되던 Major Rework 세 항목을 정리하고, 실제 감수 흐름에 맞춰 Revision Focus의 표시 순서를 바꿨다.

## Work Completed - Major Rework 중복 제거와 Revision Focus 순서

- Major Rework에서 Rebuild Academic Plan, Rebuild Conclusion, Add Motivation Bridge를 제거해 네 작업만 남겼다. 입력 요약, Dashboard 중앙값·추세·최근 기록의 분모를 4로 바꿨다.
- 과거 localStorage·JSON 백업에서 제거된 체크값을 읽을 때 버리고 나머지 작업은 유지한다. CSV에서는 제거된 열을 내보내지 않는다.
- 언어별 Academic Plan·Conclusion Rebuild 비율은 Major Rework 체크 대신 각각의 Revision Focus `rebuild` 값으로 계산한다.
- Revision Focus의 입력 표, Dashboard 통계, CSV 열 순서를 Experience Closing → Academic Plan → Conclusion으로 바꿨다. 1280px Dashboard 분석 카드는 한 열로 배치해 기존 Problem Type 이름의 잘림을 해소했다.
- 프로덕션 빌드, 단위 테스트 12개, Chrome 브라우저 테스트 8개가 통과했다. 1280px·2048px에서 입력 및 분석 화면을 확인했고 가로 넘침·콘솔 오류가 없었다.

## Decisions - Major Rework 중복 제거와 Revision Focus 순서

- J님이 기존 세 체크값의 소실을 허용했으므로 별도 이전 입력으로 옮기지 않는다. 단, 오래된 기록 전체가 열리지 않는 일은 피하도록 읽기 단계에서 해당 키만 제거한다.
- 섹션별 재작성 여부의 기준은 Revision Focus로 일원화하고, Major Rework에는 독립적인 작업 유형만 남긴다.

## Current Status - Major Rework 중복 제거와 Revision Focus 순서

로컬 기능과 자동·시각 검증을 완료했다. J님의 실제 저장 기록·EDM 폴더 파일과 배포된 앱은 확인하지 않았다.

## Next Steps - Major Rework 중복 제거와 Revision Focus 순서

- 배포 후 실제 기존 기록과 EDM 백업을 열어 네 작업만 표시되는지 확인한다.

## Work Summary - 입력 기본값 조정

새 감수 기록의 반복 입력을 줄이기 위해 Word Limit과 Surface English Quality의 시작값을 조정했다.

## Work Completed - 입력 기본값 조정

- 새 기록의 Word Limit을 입력된 값 `500`으로 시작하도록 하고 예시 placeholder를 제거했다.
- 영어 초안의 Surface English Quality를 `Average`로 시작하도록 했다. 한국어에서 영어로 전환할 때 선택값이 비어 있으면 `Average`를 적용한다.

## Decisions - 입력 기본값 조정

- 기본값은 새 기록 생성 경로에만 적용한다. 기존 기록을 열 때는 저장된 값을 그대로 불러온다.

## Current Status - 입력 기본값 조정

프로덕션 빌드, 단위 테스트 10개, Chrome 브라우저 테스트 8개가 통과했다. 이번 변경은 로컬 코드에 반영했고 배포 앱에서는 아직 확인하지 않았다. 첫 단위 테스트 시도는 샌드박스의 Vitest 설정 파일 읽기 거부로 시작하지 못해 권한을 높여 재실행했다.

## Next Steps - 입력 기본값 조정

- 새 기록 화면에서 기본 선택과 저장값을 확인한다.
