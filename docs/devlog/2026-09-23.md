# 2026-09-23 SOP Score Tracker Development Log

## Work Summary

첨부된 SOP Score Tracker 계획서와 시안을 기반으로, 감수 직후 빠르게 기록하고 언어별 구조·수정 패턴을 비교할 수 있는 데스크톱 웹앱을 구현했다. J님이 정정한 Time Spent 기본값은 60m로 반영했다.

## Work Completed

- React·Vite·TypeScript·Tailwind 기반의 단일 화면을 만들고 입력 폼, Dashboard, 분석 탭, 최근 기록 테이블을 구현했다.
- Draft Language는 English, Level은 Master's, School Tier는 Mid, AI Usage는 Yes, Time Spent는 60m로 시작하고 저장 후 새 폼에도 같은 기본값을 적용했다.
- Structure 8개 점수, Major Rework 7개, Problem Type과 Unclassified 메모 검증을 구현했다. Type 1과 Type 2는 동시에 선택되지 않도록 했다.
- 감수일과 생성·수정 시각을 분리하고, 중앙값·항목별 평균·수정 비율·복합 필터를 구현했다.
- localStorage 저장, 수정·삭제, JSON 백업·복원, CSV 내보내기와 백업 필요 표시를 구현했다. 손상된 데이터는 자동으로 덮어쓰지 않도록 했다.
- GitHub Pages 배포 워크플로와 사용 안내를 추가했다.
- 단위 테스트 5개와 Chrome 브라우저 회귀 테스트 2개를 추가했다. TypeScript·프로덕션 빌드, Chrome 1674×941 및 1280×900 브라우저 동작을 확인했다.

## Decisions

- 개인용 한 PC 앱이라 서버 없이 브라우저 저장을 사용했다. 나중에 저장 방식이 바뀌어도 기록 구조를 유지할 수 있도록 JSON 백업에 스키마 버전을 넣었다.
- 영어·한국어 비교에서 총점과 재작업 건수는 중앙값, Structure 개별 항목은 소수점 첫째 자리 평균으로 표시했다. 시간 구간은 실제 분의 평균으로 환산하지 않고 순서형 중앙 구간으로 표시했다.
- 시안에 없는 Problem Type·백업 기능은 입력 속도와 시안의 밀도를 유지하도록 기존 카드·버튼 스타일로 확장했다. 작은 화면에서는 저장 바를 두 줄로 배치했다.
- 초기 시각 검증에서 저장 바가 화면 아래로 밀리는 문제를 발견해 앱 높이와 독립 스크롤을 수정했다. 브라우저의 favicon 404 오류도 데이터 URL 아이콘으로 해결했다.
- npm 감사에서 확인된 개발용 Vitest 취약 버전을 수정 버전으로 올렸고 재검사에서 취약점 0건을 확인했다.
- Playwright 테스트 파일 추가 후 Vitest가 이를 단위 테스트로도 수집해 실패했다. Vitest의 수집 범위를 src의 단위 테스트로 제한하고 두 테스트 명령을 다시 통과시켰다.

## Current Status

로컬 Chrome에서 기본값, 필수값 검증, 저장·필터·수정·삭제·JSON 백업·복원·새로고침 후 보존을 확인했다. 1280×900에서 저장 버튼이 보이고 페이지 전체 가로 넘침이 없다. GitHub Pages Actions 배포가 성공했고 공개 주소를 Chrome에서 열어 화면이 정상 렌더링되는 것을 확인했다. J님의 실제 감수 입력 시간은 아직 확인 전이다. Actions는 기존 버전 액션의 Node 20 지원 종료 경고를 표시했지만 이번 빌드와 배포에는 영향을 주지 않았다.

## Next Steps

- J님이 실제 감수 사례로 한 건을 입력해 30초 목표와 항목 설명의 적합성을 확인한다.
- GitHub Actions의 Node 20 경고는 해당 액션의 새 버전이 제공될 때 갱신한다.

## Work Summary - Chrome 100% 가독성

Windows 표시 배율 125%를 유지하면서 Chrome 확대 100%에서 앱을 읽기 쉽도록 작은 UI 글씨를 조정했다.

## Work Completed - Chrome 100% 가독성

- 입력·버튼·점수·체크 항목과 대시보드 주요 레이블을 14~16px 중심으로 키웠고, 보조 설명도 대체로 12~13px로 올렸다. 입력칸과 선택 버튼의 높이도 함께 늘렸다.
- 1600px 이하에서는 점수/재작업 영역을 세로로, KPI는 3열·차트는 2열로 재배치해 확대된 글씨가 좁은 칸에 눌리지 않게 했다.
- 1280px·2048px Chrome 뷰포트에서 주요 글씨 크기, 저장 버튼 노출, 페이지 가로 넘침을 확인하는 브라우저 회귀 테스트 2개를 추가했다.
- 프로덕션 빌드, 단위 테스트 5개, Chrome 브라우저 테스트 4개가 통과했고 두 뷰포트의 스크린샷을 직접 확인했다.

## Decisions - Chrome 100% 가독성

- 브라우저 확대를 강제하지 않고 앱 자체 글씨·터치 영역을 키웠다. 좁은 화면에서는 이전의 9px 축소 규칙을 후행 가독성 규칙으로 덮고 카드 열 수를 줄여 읽기 크기를 유지했다.

## Current Status - Chrome 100% 가독성

로컬 변경과 자동·시각 검증은 완료했다. 실제 J님 모니터에서의 체감 가독성은 아직 확인 전이다.

## Next Steps - Chrome 100% 가독성

- 배포 후 J님 환경의 Chrome 100%에서 글씨와 스크롤 양이 적절한지 확인한다.

## Work Summary - 가독성 프로젝트 지침

작은 글씨 때문에 같은 UI 작업을 반복하지 않도록, 폰트와 레이아웃 검증 기준을 프로젝트의 `AGENTS.md`에 명시했다.

## Work Completed - 가독성 프로젝트 지침

- 프로젝트 루트에 `AGENTS.md`를 추가하고 J님의 Windows 125%·Chrome 100% 사용 환경, 권장 글씨 크기, 2048 px·1280 px 화면 검증 기준을 기록했다.

## Decisions - 가독성 프로젝트 지침

- 개인적인 연령·건강 상태가 아니라 실제 사용 조건과 읽기 편한 UI 기준을 적었다. 좁은 화면에서도 글씨를 줄이기보다 배치를 바꾸도록 했다.

## Current Status - 가독성 프로젝트 지침

지침 파일은 작성했으며, 실행 코드나 기존 UI 동작은 변경하지 않았다.

## Next Steps - 가독성 프로젝트 지침

- 이후 UI 변경 때 이 기준으로 Chrome 100% 화면을 확인한다.
